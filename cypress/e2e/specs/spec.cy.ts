describe('Cypress-TS', () => {
    beforeEach(() => {
    });
    it('Cypress commands', () => {
        // from your cypress/e2e/spec.cy.ts
        cy.visit('https://example.cypress.io/commands/actions');
    });
    afterEach(() => {
    });
});
